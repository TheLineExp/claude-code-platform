<!-- UNIFIED cross-repo QUALITY-SWEEP backlog. Maintained by the /todo skill (~/.claude/skills/todo/SKILL.md).
     Spans ALL FleetManager repos: reservations / FM V3 / vouchers / azure-ops.
     This file lives in the claude-code-platform repo (backlog/TODO.md) and is git-synced
     across every dev machine. The /todo skill pulls before reading and commits+pushes after writing.
     SCOPE: small, well-defined, lower-pri fixes/tweaks + ops/cleanup — groupable into a quality sweep.
     Distinct from: /feature (LARGE, undefined projects — backlog/FEATURES.md) and MASTER_PLAN.md (phase tracking).
     Item format:  - [ ] (ID) [repo-or-area] **title** — short note / link
     ID scheme: FF# = fast follow-up/fix, OPS# = ops/cleanup. Keep IDs stable; never renumber.
     Mark done: change [ ] → [x] and move under "Done (recent)" with a YYYY-MM-DD stamp. -->

# 🗂️ Backlog — quality-sweep tasks across all FleetManager repos

_Last touched: 2026-06-28_

## 🔥 Fast follow-ups
_(small, discrete, ship-soon items — fixes, tweaks, residuals)_

- [ ] (FF1) [reservations] **Early pickup: surface the same-day shift in the public slot builders** — backend accepts post-cutoff same-day full/half/hourly (shift to next open morning), but `computeFullDayBlocks` / `sameDayBlockGraceExpired` / hourly-future-only still drop those slots, so a customer can't select them. Feature gap, not a regression (those were never publicly bookable). Make the slot builders keep post-cutoff shifted slots when `earlyPickupEnabled`. (Codex #94 on PR #1086.)
- [ ] (FF2) [reservations] **Early pickup: DB race trigger ignores physicalStartTime** — the `20260513000000_p2_idempotency_race_safety` trigger enforces no-overlap on `Reservation.startTime/endTime` only, NOT `physicalStartTime`. Concurrent creates whose billed windows don't overlap but whose early-pickup evening HOLDS do could both pass. Pre-existing since x20 same-day early pickup; app-level `holdOverlapOR` covers the normal path. Fix: extend the trigger (migration) to also enforce `physicalStartTime`-based overlap. (Codex #89 on PR #1086.)
- [ ] (FF3) [reservations] **Public booking: explicit-hourly resume key mismatch** — `useDateSelection.js` hydrates `timeBlockKey` to a numeric start-hour, but explicit hourly `BookingSlot` keys are strings, so on resume of a saved explicit-hourly booking the start-time card shows "Start time…" (not "Starts X") and the `SET_DATES` hourly branch can fail to re-commit. Pre-existing in the hook's hydration; surfaced by the flattened block grid (PR #1101). Fix: add a `matchedKeyRef`-style match (as in FM V3 `ScheduleEditor`) that maps the incoming startTime to a resolved hourly slot key once slots resolve.
- [ ] (FF4) [reservations] **Build the last two bug-family detectors — F2 (flag-threading) + F9 (stale-async)** — the remaining 🔶 families in `docs/BUG_FAMILIES.md`. Build each via the draft→Codex-to-zero loop (Engine 2). F2: a flag/bypass honored at one gate but re-rejected by a downstream sibling (the `allowClosedDays` / `getStripeContext`-cutover class). F9: an in-flight async write with no generation token, or a `useState(initial)` that never re-syncs across a refetch. `harvest-codex-findings.js` ranks them when they recur. Detectors F1/F4/F5/F6/F7/F8/F10 already shipped.
- [ ] (FF5) [reservations] **Auto-hourly per-hour tier (hour_2) parity in 2 scoped-out sites** — PR-8a (#1186) shipped `resolveAutoHourTier` (2h→hour_2) to `_calculatePricing` + `/availability/v2` only. Mirror it in `groupService.computeGroupPricing` (staff bulk/CSV group create — a 2h group still bills hour_1/ambiguous from a raw `durationType:'hourly'` fetch) and `fleetReadinessService` readiness checks for full consistency. Low priority: no public cart, separate flows; not a cart≠charge regression.
- [ ] (FF7) [reservations] **agentService.createBooking `_agent.summary` hardcodes "Booking confirmed!"** — even when the auto-confirm failed (card/waiver gate threw → status stays `pending`), the agent-facing `_agent.summary` string still says "Booking confirmed!". Pre-existing cosmetic on the agent API surface (the notification double-send half of this was folded into C6 #1209 as FF6). Make the summary reflect the actual `reservation.status` (e.g. "Booking received — pending …" when not confirmed).



- [ ] (FF11) [reservations + FM V3] **Deprecate & eventually expire the legacy `half_day` tier** — *step 1 SHIPPED: no new half-day for new fleets, FM V3 [#1127](https://github.com/TheLineExp/Fleetmanager_V3/pull/1127) (hide the Half-day slots section when a location has none).* — 8c-4 (cleanup) keeps EXISTING half_day slots/rules fully working (resolver still resolves the tier; named 3h/4h hourly sessions are the replacement). Owner intent (2026-06-24): existing fleets keep half_day, but NEW fleets should not get it going forward, and eventually retire it entirely. Phased: (1) stop offering `half_day` as a slot category / hide it in the FM V3 admin for fleets with no existing half_day slots; (2) audit live half_day slots/rules in prod; (3) migrate those fleets to named `hour_3`/`hour_4` 'Half Day Morning/Afternoon' sessions; (4) remove `half_day` from `PRICING_TIERS_ACCEPTED` + rework the resolver to read it from `PRICING_TIERS_FULL` (readable) so existing rows never un-price during the wind-down. NOTE: a naive ACCEPTED removal today un-prices existing half_day slots (resolver iterates ACCEPTED) — that's why it's staged. May graduate to an FR if the migration step needs design.
  - **8c-4 attempt (CLOSED — premature):** reservations [#1250](https://github.com/TheLineExp/fleetmanager-reservations/pull/1250) + FM V3 [#1126](https://github.com/TheLineExp/Fleetmanager_V3/pull/1126) tried to remove the half-day auto-split but Codex confirmed it's still LIVE in 3 paths (not dead): (a) public `useDateSelection.handleStartNow` walk-in sets `durationType:'half_day'`+`afternoon` with NO slot check → relies on auto-computed blocks; (b) `categorySlotPresence` is location-wide so day-scoped half_day slots show the pill on off-days, and backend `bookingSlotMatch.js` still treats no-slot-that-day as the valid auto-compute regime (FE/BE parity); (c) FM V3 staff scheduler `scheduleBlocks.scheduleForDay` reads `entry.halfDaySplit` (midpoints only when missing) so dropping it on save shifts existing custom Morning/Afternoon windows. The removal must migrate ALL of these (incl. backend) before the auto-split can go — that's this FF.
- [ ] (FF12) [FM V3] **Inline-error consistency sweep for long reservations settings pages** — give the other long composer/settings forms the same fix shipped for VolopassOfferings ([#1136](https://github.com/TheLineExp/Fleetmanager_V3/pull/1136)): render save/validation errors inline next to the Save button + a success banner, instead of a top-of-page `error-message` div that scrolls off-screen on a long form (makes "Save" look like it did nothing). Migrate to `AlertBox` per the mandatory Action-Feedback Pattern. Candidates in `frontend/src/pages/reservations/settings/`: PricingSettings, WaiverSettings, BookingSlotsPanel, SettingsDamageWaiver, ContentSettings, RentalPoliciesSettings, CouponSettings, FieldConfigSettings, AddOnsSettings (audit each for the off-screen-error pattern before changing).
- [ ] (FF13) [reservations] **Thread the effective `week` tier into voucher coverage** — weekly multi_day bookings resolve voucher coverage via `LEGACY_TO_TIER['multi_day']` (=full_day), so a voucher/pass scoped to the `week` tier is wrongly rejected (or treated as upgrade/hidden) for a 7-day rental even though it's priced at the weekly rate. Needs `pricingResolver.computeMultiDayBikePrice` to expose WHICH multi-day tier bound (week binds when `weekPrice!=null && days>=7`, unless the month cap undercuts), then thread that effective tier through the 4 voucher-tier sites in `reservationService.js` (`effectiveVoucherTier` ~12471, `_resolveAutoHourVoucherTier` ~12841, baseline ~4054, recompute ~4382). Money-path → careful tests. (Codex P2 on prod release [#1263](https://github.com/TheLineExp/fleetmanager-reservations/pull/1263).) LATENT (only once a fleet enables the week block AND a week-tier voucher exists). Context: memory `ff12-month-cap-toggle.md`.
- [ ] (FF14) [reservations] **Filter explicit hourly slots by the enabled per-hour tier in the public picker** — `enabledHourOptions` only narrows the auto-hour card list; the `hasExplicitHourly` branch + `StartTimeSheet` still render every explicit hourly `BookingSlot`. Since `assertBlockEnabled` gates every hourly booking by its billed hour-count, a fleet with e.g. `hour_2` disabled but an existing 2h explicit slot offers it then 400s `BLOCK_DISABLED` at checkout. Filter `hourlySlots`/explicit slots by the enabled hour tier (DatesSection + StartTimeSheet). (Codex P2 on #1263.) LATENT (explicit hourly slots + a disabled per-hour tier). Adjacent to FF3.
- [ ] (FF15) [reservations] **Thread the effective `week` tier into the scoped-flow custom_table override** — `scopedFlowService.applyPricingOverride` is called with `pricingTier: durationType` (`reservationService.js` ~12241), and it normalizes `multi_day`→`day_2`, so a `custom_table` override keyed to the `week` tier never applies on weekly scoped-link bookings. Compute effective tier `week` when the weekly rate bound (same signal as FF13) + pass it to `applyPricingOverride`. Narrow (scoped link + custom_table + whole-week booking). (Codex P2 on #1263.) LATENT.
- [ ] (FF16) [reservations] **Gate catalog browse prices on `bookingBlockConfig`** — the catalog browse path (`public.js` ~1607 `priceFrom`/`pricingTiers`, + `minFromPriceForRules` ~1820) builds a "from" price from every active PricingRule without loading `bookingBlockConfig`. A fleet that disables a cheap block (Hourly/Monthly) but leaves its rule in place advertises an unavailable lower "from" that the picker hides / checkout rejects. Load block-config + filter catalog tiers like the quote/charge paths. (Codex P2 on #1263; the two date-less month-cap previews at numDays:2 are already commented as intentionally inert.) LATENT (disabled block + leftover rule).
- [ ] (FF17) [reservations] **Add an "SMS notifications" toggle to the Notifications settings UI** — `TenantConfiguration.smsNotificationsEnabled` (@default(false)) has NO frontend control (zero refs in `frontend/src`); only settable via `PUT /:fleetId/notification-config` (whitelisted at notificationSettings.js:157) or DB. This hidden toggle being **off** was the ROOT CAUSE of The Line's 2026-06-28 "ClickSend isn't working on prod" incident — SMS was gated off at the fleet level in `reservationNotificationService.sendNotification` before any ClickSend call (ClickSend creds/templates were fine; it sat off through the Twilio-blocked months and was never flipped). Fixed in prod by DB write 2026-06-28 (verified: SMS sending). Add the UI control so it's self-serve. Context: memory `clicksend-sms-disabled-toggle-not-creds.md`.
- [ ] (FF18) [reservations] **Seed `return_reminder` + `ready_reminder` EMAIL templates** — both reminder keys have only an SMS `MessageTemplate` (no `email` channel row) for The Line, so a phone-less customer gets NO reminder on any channel (e.g. prod scheduled msg `812aafc1` — return_reminder, empty phoneNumber, has email, aged out → failed). Add email templates for these keys (and audit other reminder keys for SMS-only gaps) so email is a real fallback when SMS can't deliver. Surfaced during the 2026-06-28 ClickSend incident.
- [ ] (FF19) [reservations] **Add a `_pingClickSend` probe to verbose `/health` + fix the scheduler's misleading "not configured" log** — verbose `/health` has `_pingTwilio`/`_pingResend` but NO ClickSend probe, so an SMS-provider problem is invisible from outside (the 2026-06-28 incident was only visible in scheduler logs). Add a global-cred ClickSend liveness probe mirroring `_pingTwilio`. Also: `notificationSchedulerJob` logs `"(likely SMS/email provider not configured)"` as a hardcoded GUESS — it sent debugging down the wrong path when the real cause was `smsNotificationsEnabled=false`. Make it report the actual reason (channel disabled / no template / no recipient / provider).

## 🧹 Ops / cleanup

- [ ] (OPS2) [reservations] **Manually refund the accidental add-on (~$0.93) on res `d606e299-17c6-4847-bbda-41d81055ebd4`** — The Line (fleet `00a09903-34dd-4a5f-a1d0-cf34147c3375`). Customer accidentally got an add-on added; refund it via the modal ("Return to original card") **once the refund-'paid' fix is LIVE IN PROD**. This `paymentStatus='paid'` (Checkout-paid) reservation is the repro that surfaced the bug. **Blocked until prod:** staging [#1280](https://github.com/TheLineExp/fleetmanager-reservations/pull/1280) MERGED; prod [#1281](https://github.com/TheLineExp/fleetmanager-reservations/pull/1281) OPEN→master (merge + deploy needed first). (Mike: hold, do manually post-deploy.)
- [ ] (OPS1) [reservations] **Promote Engine-2 (Codex-early draft review + shipit draft-flow) to prod/master** — detectors are already on prod via #1128; this completes the prevention system on `master`. Dependency: the Engine-2 commits edit `codex-auto-request.yml` / `claude-auto-review.yml` / `REVIEWS.md`, which build on the staging-only "local-first review model" base (commits `30f1e5d4`/`fe9f34ea`) and `REVIEWS.md` is absent on master — so cherry-pick that base to the release branch alongside Engine-2. On staging now (PRs #1120 / #1122 merged).

## ✅ Done (recent)
<!-- move completed items here with a date; prune periodically -->
- [x] (FF10) [reservations] **Gate reschedule/self-modify for max-rental-length + block-enable** — shipped via [#1252](https://github.com/TheLineExp/fleetmanager-reservations/pull/1252): modifyPublicReservation now runs assertBlockEnabled + maxRentalDays day-span on customer self-modify (window-changed only); staff paths exempt. (2026-06-25)
- [x] (FF8) [reservations] **hour_3/hour_4 opt-in in the booking-block commit gate** — merged to staging via [#1237](https://github.com/TheLineExp/fleetmanager-reservations/pull/1237) (incl. Codex P2 normalize fix). Aligns the gate with both FEs. (2026-06-24)
- [x] (FF9) [FM V3] **SelfServiceSettings sends only its owned fields (no config clobber)** — merged to staging via [#1122](https://github.com/TheLineExp/Fleetmanager_V3/pull/1122). (2026-06-24)
- [x] (FF6) [reservations] **FOLDED INTO C6 (#1209)** (2026-06-24) — agentService.createBooking double booking-confirmed notification when `requireCardOnFile` is ON. Per M ruling 2026-06-24, NOT deferred: the publish-ownership gate is now in the same notify block. agentService fires its own `sendBookingConfirmation`/`scheduleReminders`/`sendPostBookingRiderEmails` only when the confirm succeeded AND `confirmReservation` did not own the publish (`preConfirmStatus !== 'pending_payment' && !_fleetRequiresCardOnFile(fleetId)`) — mirrors `public.js`'s `!cardOnFilePublishOwnedByConfirm` gate, same predicate `confirmReservation` keys off so they can't drift. Test added (requireCardOnFile ON → agentService does NOT publish). Ships within C6 [#1209](https://github.com/TheLineExp/fleetmanager-reservations/pull/1209).
